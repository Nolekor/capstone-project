import { useContext } from "react";
import Image from "../components/Image";

const Cart = () => {
  const { cartItems } = useContext();
  return <main className="cart-page">images go here</main>;
};

export default Cart;
